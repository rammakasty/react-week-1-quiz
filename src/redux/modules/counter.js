const inintialState = {
    number: 0,
};

const counter = (state = inintialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default counter;
